{
  "schema_version": 2,
  "worker_slot_transitions": {
    "free": ["claimed", "canceled"],
    "claimed": ["in_progress", "free", "blocked", "canceled"],
    "in_progress": ["blocked", "submitted_for_review", "canceled"],
    "blocked": ["in_progress", "free", "canceled"],
    "submitted_for_review": ["changes_requested", "approved", "canceled"],
    "changes_requested": ["in_progress", "submitted_for_review", "canceled"],
    "approved": ["merged", "changes_requested"],
    "merged": ["completed"],
    "completed": [],
    "canceled": []
  },
  "worker_lease_transitions": {
    "not_acquired": ["active"],
    "active": ["expired", "released", "revoked", "orphaned"],
    "expired": ["recovered", "revoked"],
    "orphaned": ["recovered", "revoked"],
    "released": [],
    "revoked": [],
    "recovered": ["not_acquired", "active"]
  },
  "supervisor_transitions": {
    "unassigned": ["active"],
    "active": ["expired", "relinquished"],
    "expired": ["active"],
    "relinquished": ["active"]
  },
  "supervisor_lease_transitions": {
    "not_acquired": ["active"],
    "active": ["expired", "released", "revoked", "orphaned"],
    "expired": ["recovered", "revoked"],
    "orphaned": ["recovered", "revoked"],
    "released": ["active"],
    "revoked": ["active"],
    "recovered": ["active"]
  },
  "invariants": [
    "active Worker slots require selected identity-verified claimant, claim_ref, claimant_identity_ref, claim_id, coordination_epoch, worker fencing token, active lease and expiry",
    "Worker claim requires a live authoritative Supervisor epoch and complete typed handoff envelope",
    "SUPERVISOR_ONLY or capability-restricted work cannot be claimed by unauthorized Workers",
    "active Supervisor requires selected identity-verified agent, identity_ref, lease_id, active lease, fencing_token, election_ref and expiry",
    "only the current Supervisor fencing token may perform shared coordination mutations",
    "expired or orphaned locks require evidence reconciliation before reuse",
    "coordination ref namespaces are protected infrastructure and require trusted runtime authorization",
    "repository reality overrides stale mirrored status"
  ]
}
