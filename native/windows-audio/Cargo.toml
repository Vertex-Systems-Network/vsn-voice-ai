[package]
name = "vsn-windows-audio"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
vsn-audio-core = { path = "../audio-core" }

[target.'cfg(windows)'.dependencies]
windows-core = "0.62.2"
windows = { version = "0.62.2", features = [
  "Win32_Foundation",
  "Win32_Media_Audio",
  "Win32_Security",
  "Win32_System_Com",
  "Win32_System_Com_StructuredStorage",
  "Win32_System_Threading",
  "Win32_System_Variant",
] }
